- name: ansible from variable
  hosts: local
  connection: local
  vars:
    person_name: "vamsi"
    height: "6"
  tasks:
  - name: print the variable
    ansible.builtin.debug:
      msg: "{{person_name}}, height is {{height}}"